# 常见故障 Runbook：CPU 负载与异常进程

## CPU 高 / 负载高 处置流程
1）uptime 查看 1/5/15 分钟平均负载；2）top 或 ps 找出 CPU 占用最高的进程；3）结合业务判断是否为预期峰值；4）对确属异常的进程，先尝试降级或限流，必要时在确认后 kill 指定 PID。严禁无差别 killall 或对系统关键进程发送信号。

## 僵尸进程 / defunct 处置
僵尸进程（Z/defunct）本身不占资源，根因是父进程未回收子进程。处理方式是向父进程发送 SIGCHLD 或重启父进程，而非直接 kill 僵尸进程本身。
